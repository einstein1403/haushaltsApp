# Environment Configuration Example
# Copy this file to .env and configure with your actual values

# CRITICAL: Generate a secure JWT secret (minimum 32 characters)
# Use: openssl rand -base64 32
# Never use the default value in production!
JWT_SECRET=your-super-secure-jwt-secret-here-minimum-32-chars

# Database Configuration - für Docker Compose
DB_PASSWORD=your-secure-database-password

# Alternative Database URL für lokale Entwicklung
DATABASE_URL=postgresql://postgres:${DB_PASSWORD}@localhost:5432/household_tasks

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# React App Configuration
REACT_APP_API_URL=/api